# 라이브러리 버전
# BaseEntity 의 Q 클래스를 만드는 데 씀
# 서비스 쪽 service-template 의 값과 반드시 같아야 함
querydslVersion=7.6

# 빌드 결과를 재사용해 다시 빌드할 때 시간을 줄임
org.gradle.caching=true

# 서로 의존하지 않는 작업을 동시에 실행함
org.gradle.parallel=true
